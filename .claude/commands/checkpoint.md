---
description: Record progress after a step (update the live plan status + experiment logs)
argument-hint: "[short note on what we just did]"
---
Record where we are, following the **Checkpoint** workflow in `skill/workflows.md`: update status in
`draft6.0/idea/main.ideas.v1.md` (and the experiment workspace if a run happened), keep that status as the
single source of truth (others point to it, not restate it), respect the write-boundary (don't log runs in
the `ideas1.md` derivation chapters), then suggest `/commit-progress`.

What we just did (optional): $ARGUMENTS
