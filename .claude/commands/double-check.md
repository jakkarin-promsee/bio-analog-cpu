---
description: Run the 3-lens consistency audit (full-context / outsider / worker) and fix drift
argument-hint: "[optional focus area]"
---
Run the **Double-Check** workflow in `skill/workflows.md`: read the front-door + touched docs fresh, grep
for drift, and report from the three lenses — full-context (what we missed), outsider (where a cold agent
falls), worker (hard to find / hard to start). Fix the unambiguous breaks; flag the judgment calls. Be
honest — a found problem is the point, not a failure.

Focus (optional): $ARGUMENTS
