---
description: Re-read and re-sync on the project (optional focus: code | experiment | arch)
argument-hint: "[code | experiment | arch]"
---
Re-orient on this project and re-sync me, following the **Orient** workflow in `skill/workflows.md`.

Focus area (optional): $ARGUMENTS — if given (`code` | `experiment` | `arch`), also load that lens's map.

Keep it tight: where we are, the frame, what's solid vs open, what to watch next, and flag any doc drift
you notice. Orient, don't lecture.
