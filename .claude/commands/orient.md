---
description: Re-read and re-sync on the project (optional focus: code | experiment | arch)
argument-hint: "[code | experiment | arch]"
---
**Orient** — re-read & re-sync. The "get me up to speed / re-think before we continue" move.

1. Skim the loaded `CLAUDE.md` + `draft6.0/CLAUDE.md` (frame + status). For the concept frame use the `project-frame` skill; to navigate, the `explore` skill.
2. Read the live plan: `draft6.0/idea/main.ideas.v1.md` (decisions + status) and the rung in `ideas1.md`. Whole picture cold → `draft6.0/context.md`.
3. If a focus arg ($ARGUMENTS) is given, load that lens's draft-6 skill — **code** → `simulator-code`; **experiment** → `run-experiment`; **arch** → `project-frame` + the decision record.
4. Report **tight**: where we are · the frame · what's solid vs open · what to watch next. Flag any doc drift you notice. Orient, don't lecture — don't restate the whole spec.

Focus area (optional): $ARGUMENTS
