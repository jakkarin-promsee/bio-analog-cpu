---
name: find-paper
description: Find the paper behind a draft-6 decision — the per-phase paper indexes + the survey. Use for "find the paper behind", "which paper covers", "the SCFF paper", "the boosting paper", "what is the evidence for X", "what does paper Y say".
---

# Finding a paper

**The two index READMEs — read these to find the right story file, then open *that one file*:**
- `draft6.0/research/papers/phase1-2/README.md` — the adopted **design** papers (SCFF · DeeperForward · Distance-Forward · BoostResNet · BYOL · LLRD).
- `draft6.0/research/papers/phase3/README.md` — the **depth-reframe** papers (the objective reframe, the 3 coordination directions, and the master paper list: GIM, CLAPP, Mono-Forward, …).

**For "what else was considered":** `draft6.0/research/survey/summary.detail.md` (the learning-rule zoo).

**Off:** `draft6.0/research/north-star/` is free-time reading (the recurrent-brain dossier) — don't open it to answer a design question.

**Budget:** ≤3 index files (the two READMEs + the survey summary). **Never open a story file just to find which one is relevant** — the index gives you the path.
