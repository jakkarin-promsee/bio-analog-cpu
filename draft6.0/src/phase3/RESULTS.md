# Phase 3 — Results ledger

> The **synthesis surface** for the depth-round-2 / objective-reframe phase — one row per rung, accumulated as
> they land. Reporting format: [`result-format.md`](result-format.md). Pre-run design: [`design.md`](design.md). Phase-2
> precedent: [`../phase2/RESULTS.md`](../phase2/RESULTS.md).
>
> **Fill a row the moment a rung's *decision* is made.** Phase-3 scalars: the **depth-slope** (probe change/layer;
> +ve = depth helps) + **decline-area / tail-retention** (the shape) + **selectivity** (is the preserved info
> class-relevant, vs a random projection) + final-probe median. The n=5 "disjoint-IQR + ≥4/5 seeds" rule holds.

| Exp | Commit | Headline scalar (slope · shape · selectivity) | Reference (GD ceiling / chance) | Knob / verdict it set | Decision it fed | Card |
| --- | --- | --- | --- | --- | --- | --- |
| **P3.3** digits (continual veto) — _closes Phase 3_ | _wip_ | **3-seed, VETO PASSED (and IMPROVED).** Does `contrast + coordination` re-earn the Phase-1 continual win? **Yes, and beats the energy baseline:** contrast+coord+sleep final **0.954**, BWT **−0.017** [−0.020,−0.015] vs energy-SCFF+sleep 0.938 / −0.026 [−0.027,−0.022] (disjoint-IQR, 3/3); contrast SCFF-probe **FLAT** 0.938→0.936 (doesn't forget); sleep decisive (no-sleep rots to 0.214; GD-online 0.186/−0.992). | energy baseline 0.938/−0.026; GD 0.186; chance 0.10 | **The objective reframe re-earns AND improves the continual win** — the contrastive cell is forgetting-robust like energy-SCFF (per-sample, no batch stats → continual-safe by construction); the InfoNCE-bias-to-current-task worry didn't materialize. The better static objective helps continual too. | **ADOPT the reframe → Phase 3 CLOSES.** Recipe = `[contrast (InfoNCE) + coordination w≥2] bulk + sleep-consolidated readout` — supersedes energy-goodness as the SCFF objective. Hand-off to Phase 4 (gate + sleep-cadence) tunes against this cell. | [P3.3](exp3/experiment-3.md) |
| **P3.2** headroom (side road A: depth-headroom confirmation) | _wip_ | **5-seed, DECISIVE PASS — given headroom, the method COMPOSES depth.** Built a flat-MLP task with real depth headroom (`make_tierb` grid4/n_active3/overlap0.6/64-cluster; GD-hidden RISES 0.39→0.51). On it: energy-wall **declines −0.0055** (the wall); contrast **w1 +0.0010 (rise-to-L3 then FALLS — myopic)**; **w2 +0.0124 (rises)**; **w4 +0.0220 (rises MONOTONE, 0.41→0.569, tail 0.99, sel +0.181)** — w4's deep probe (~0.57) **exceeds** the fixed-budget GD (~0.52). Monotone w1<w2<w4 every seed. | GD-hidden **RISES** 0.39→~0.51 (headroom ✓); random declines; chance 0.10 | **Reframe + Direction 1 VALIDATED on the static axis.** P2.2's "depth intrinsic to forward-only locality" **overturned** — intrinsic to the *energy objective*; with a discriminative objective + coordination, forward-only unsupervised learning composes depth. Coordination is THE lever (w1 rises-then-falls → w4 climbs). P3.1's flat-CIFAR PARTIAL = a *task-ceiling* artifact, not a method limit. *(Caveat: synthetic task; GD = fixed-budget baseline — the rising SLOPE is load-bearing, not the GD-beat.)* | **Carry `contrast + coordination (w≥2)` → P3.3 (continual veto)** — the project-deciding question. Direct-feedback (side road B) now lower-value (window already earns the rise). | [P3.2](exp2/experiment-2.md) |
| **P3.1** cifar (coordination window on contrast) | _wip_ | **5-seed, PARTIAL — Direction 1 works but flat-CIFAR has no headroom.** Coordination window on the contrastive cell: **w1** (=P3.0 contrast, regression guard ✓) slope **−0.0158**; **w2** (coordination) **−0.0105** (~33% flatter, deep endpoint 0.230→**0.254**, sel +0.059→**+0.066**); **w4** saturates (−0.0113). **Synth (headroom): w1 −0.005 → w2 +0.002 → w4 +0.008 (RISES).** Monotone, 5/5. *(First launch died after 4/5 seeds — laptop slept mid-run, no crash; re-ran clean.)* | GD-hidden **flat ~0.36** (no rise — no depth headroom for *anyone*); energy −0.0202; random 0.30→0.16; chance 0.10 | **Coordination (the user's Direction 1) is a real, correct lever** — eases the decline, lifts deep layers, holds selectivity, *rises* where the task has headroom (synth). But flat-CIFAR can't show "rising" (GD flat too) ⇒ **slope ≥ 0 is the wrong bar here**; the achievable win (stop destruction, preserve class, gentle decline) is met. Saturates at w=2. | **Lock `contrast + w2` as the Phase-3 unsupervised cell → route P3.3 (continual veto)** — the question that decides adoption (architecture's home = continual, Phase 1). **Direct-feedback** = optional static side-check; a **depth-headroom task** would make "rising" testable. Mono-Forward NOT triggered. | [P3.1](exp1/experiment-1.md) |
| **P3.0** cifar (objective swap: 3 objectives) | _wip_ | **5-seed, PARTIAL — the objective family IS the lever; contrast wins, residual decline → coordination.** energy-wall reproduces the P2.1 wall (0.332→0.198, slope **−0.020**, guard ✓, decays *through* random). **masked-recon** flattens (0.174→0.136, slope −0.005) but **selectivity −0.062 (below random** — preserves density). **contrast (CLAPP/InfoNCE)** stays **above random at every depth** (0.331→0.227, **selectivity +0.060, 5/5, growing +0.035→+0.073**) — preserves **class** info — **but still declines** (slope −0.016). The two needed properties split: recon=flat-but-wrong, contrast=right-but-declining. Synth control: both pass (recon +0.079, contrast +0.105 where clusters=classes). | GD-hidden **~0.36** (env gap +0.116); random 0.298→0.160; chance 0.10 | **Reframe confirmed: the objective family is the lever; the right one is DISCRIMINATIVE preservation (contrastive), not energy or reconstruction.** Depth wall **not** intrinsic to forward-only locality — intrinsic to the *energy* objective. The residual contrast decline = the **cross-layer-coordination gap** (myopic per-layer discrimination), not an objective gap. | **Route → P3.1 (coordination = the user's Direction 1 / OLU / direct-feedback), layered on the CONTRASTIVE objective.** Contrast = the Phase-3 objective (carry `SCFFContrast`). **Mono-Forward fallback NOT triggered** (contrast cleared the selectivity bar). Masked-recon stays as proof that preservation-alone preserves the wrong thing. | [P3.0](exp0/experiment-0.md) |

_Pre-run smoke (2026-06-21, not a ledger row): synth 2-seed + CIFAR 2-seed de-risked the bench; the masked-recon
cell trains NaN-free; the energy-wall reproduces P2.1's −0.020 slope exactly (regression guard). The selectivity
control (untrained same-arch SCFFRecon) was decisive — without it, masked-recon's flat-but-low curve would have
looked like a win._
