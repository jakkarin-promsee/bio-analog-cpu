# main.ideas.v1 — Draft 6.0, the approved design

**Snapshot v1 · 2026-06-12.** This is the *decision record* — what we've committed to and why, one line each. The full story behind each call is in [ideas1.md](ideas1.md); the outside papers are in [../research/papers/](../research/papers/README.md). Where this disagrees with the draft-5.1 "locked decisions" in the top-level `CLAUDE.md`, **this wins for draft-6.0 work** — 5.1 is the pre-pivot world.

---

## The whole thing in one breath

Two brains. A cheap, unsupervised **SCFF** front (~80%) that organizes the world for free, and a small, precise **gradient-descent** back (~20%) that puts real labels on it — because direction is the one expensive thing in learning, so we pay for it *once*, where it counts. The two are chained as **residual boosting blocks**: each block a weak corrector, SCFF doing feature work inside the residual stream, a GD checkpoint at each block's exit. Learning is **threshold-gated** (cheap local SCFF most of the time; expensive direction only when the cheap path stalls) and **sleep-consolidated** (periodic full-batch GD over a prototype memory, so nothing rots).

---

## The net — the three approved calls

**N1 · SCFF stays the cheap brain.** Vanilla, unsupervised — the only rule that is local + derivative-free + forward-only + unsupervised at once. Relabel our **summation** form as *our* reformulation of the paper's concatenation (exact because `W₁=W₂`; first-layer-only; it's what makes mono-forward possible). Keep Distance-Forward's **margin loss on standby** for if the log-loss is mushy. → [scff.md](../research/papers/phase1-2/scff.md), [distance-forward.md](../research/papers/phase1-2/distance-forward.md)

**N2 · The middle layer = stability + coordination, split apart.** Don't blend two objectives on one layer — they fight. Instead: **plasticity-gradient slowdown** (mirror LLRD — slow the *late* SCFF layers GD reads) for stability, plus **DF-O overlapping blocks** for coordination. **EMA-view** (BYOL momentum encoder) is the de-risked upgrade if the cheap slowdown pinches SCFF's learning. → [llrd.md](../research/papers/phase1-2/llrd.md), [distance-forward.md](../research/papers/phase1-2/distance-forward.md), [byol.md](../research/papers/phase1-2/byol.md)

**N3 · GD = residual boosting blocks.** Each GD checkpoint fits the *residual* the previous one left (boosting telescoping sum — BoostResNet); SCFF does feature work inside the residual stream. The boosting guarantee lives on the labels → in the GD checkpoints, not the unsupervised SCFF stages. → [boostresnet.md](../research/papers/phase1-2/boostresnet.md)

---

## Supporting structure (committed)

**S1 · Path diversity comes from depth, not input-width.** Concatenation's only edge over summation was path diversity (training-time over-parameterization) — and it's *redundant* diversity that collapses to `W₁=W₂`. Depth buys it better: non-redundant capacity, uniform width-`d` rails, and it matches our own locked "path-diversity-per-scap" topology. Depth's cost is coordination debt — which N2 + N3 already exist to pay down.

**S2 · Mono-forward, dual-rail.** One forward sweep, two worlds (positive + negative) side by side through the *shared* weight crossbar. Doubles only the **LocalCapacitor** activation buffers (RAM-like temp `a` values), **not** the Scaps. Trades cheap buffer area for one charge cycle instead of two. Both goodnesses stay explicit → full two-sided loss, no scale runaway.

**S3 · GD reads via taps.** GD reads the last `n` SCFF layers (richer than one thin top layer; precedented by SCFF's own all-layer readout). GD reads, never writes into SCFF → no shared weights, and a built-in stop-gradient (BYOL's anti-collapse condition, for free).

**S4 · Two GD organs.** *Interface GD* (small, per-block exit, tracking job, SGD+momentum, squared error) vs *Output GD* (the brain, final, 2–3 layer residual MLP, Adam-class online / full-batch at sleep, cross-entropy).

**S5 · Inter-layer normalization, mandatory.** Required by FF for correctness (the next layer can't just recycle the previous layer's goodness) — and now load-bearing twice, because it's also the guard against the residual shortcut (a block inflating `‖h‖²` just by passing its input through).

**S6 · Threshold-gated learning.** Loss < θ → SCFF only (cheap, local). Loss ≥ θ → SCFF + chained GD delta. Justified by the gradient-mismatch ε-floor: local learning converges while block disagreement is small, stalls when it dominates. Candidate refinement: gate on loss-*slope* (plateau detection), not absolute loss.

**S7 · Sleep consolidation.** Periodic full-batch GD over history, re-covering the whole data range against the *current* SCFF map. Cheap by construction (GD ≈ 20% of weights, SCFF body frozen). Robustness from tap-normalization + drift-spanning replay + free analog noise augmentation.

**S8 · LUT prototype memory.** Deduplicated raw-input prototypes (winner-take-all novelty allocation). One store, three customers: SCFF negatives, sleep replay, and the seed of the future "memory model" (the hippocampus track). Experiment 3.2 becomes the prototype-history cell.

**S9 · Readout = a fixed short-stack placement (Phase-5 close-out; revises S3's literal "tap ALL layers", keeps its intent).** SCFF composes the depth a task needs (sharper InfoNCE temp 0.2 — the *earn* lever), and that depth is **read by a short, fixed truncation stack at the extractor end** (~L2–3 on the flat continual home; all-tap when peak accuracy is wanted; the deep L12 bulk reserved for compositional tasks). The **adaptive per-sample early-exit was struck** (P5.5 — the flat home rewards pooling over placement) and **no frozen residual** is carried (P5.6 skipped). S3's intent — *read multiple layers, GD never writes SCFF* — is kept; what changes is *where* to read: the sharp extractor depth, not literally every layer (all-tap dilutes the class signal with the drifted deep layers). → [`../src/phase5/README.md`](../src/phase5/README.md)

**S10 · SCFF carries a noise-aware objective (Phase-6 close-out; makes S7's "free analog noise augmentation" a committed *base* property, not a readout-side afterthought).** The base representation is hardened against analog/continual noise **in the SCFF objective, forward-only** — by corrupting **one InfoNCE view with broad (iid) noise at train time** (`NoiseAugContrast`, σ_aug=1.0). This is load-bearing because a frozen readout *preserves but cannot manufacture* backbone robustness (LP-FT), so robustness that isn't in the base can never be added by the 20% GD namer. **Scoped:** it substantially (partially) hardens the Rasch-dominant **tap** channel (retention 0.82→0.87, 5/5 paired), is **continual-safe** (the A6 win survives — BWT −0.022→−0.017) and **natural-data-confirmed**, but leaves a named **input-transducer directional residual** for Stage-2 read-side (calibration under shift; ADC<3-bit). Two Phase-6 corrections to prior intuition: the effective augmentation is **generic, not directional-specific** (broad iid ≥ random-axis > a directional-specific corruption — you cannot bet on one axis for a coherent shift unknown at train time), and the directional enemy is a **coherent translation** along the class axis (per-sample rep-cos is blind to it → **retention** is the direction-first read). Door B (the continual "all data is noise") is resolved: the class direction **forms from a fully-noisy stream** (buffer purity ≈ naive averaging, unneeded here). → [`../src/phase6/README.md`](../src/phase6/README.md)

**S11 · The GD namer is a closed-form/streaming analytic head, not gradient descent (Phase-7 close-out; SUPERSEDES N3, COLLAPSES S4, EXTENDS S9).** The ~20% "precise brain" that maps the frozen SCFF bulk → our labels is a **bake-off winner, not a GD head by assumption**: the committed namer is **RanPAC** (a frozen random ReLU projection → running-Gram ridge prototype, `W=(G+λI)⁻¹M`) **+ a class-balanced-reservoir imbalance guard** — **no gradient, closed-form, streaming.** On the continual home it ties the gradient MLP anchor on accuracy×BWT (a 3-way statistical tie {MLP, RanPAC, SLDA}, all no-gradient but MLP), leads on natural digits (0.949, #1, near-zero forgetting), passes the A6 gate (the trained cosine-softmax **and** the max-magnitude FeCAM are struck for denting A6 — the *trained weights*, not the metric, carry the recency bias), and is the most spine-clean of the tied cluster. **So the "20% GD" is a *role*, not a *method* — the 20% is not even gradient descent.** **The spine bends (honest):** the direction-pure cosine head is spine-clean (argmax-flip 0.000) but sub-competitive where the bulk has structure (**magnitude-wins-spine-bends**, Δ=0.128 synthetic; the price shrinks 4× to −0.036 on digits and ≈0 on CIFAR-flat) — the winner reads a magnitude (ridge) yet is recency-robust by having no trained weights (recency-robust ≠ direction-reading). **Two Phase-7 corrections to the plan's intuition:** the imbalance guard is **class-balanced reservoir (buffer-side), NOT AIR** (AIR over-corrects); and the "multimodality cliff" is **anisotropy, not multimodality** (natural features are unimodal — the +0.19 lever is a *tied covariance*, closed-form, no non-convex mixture). **Consequences for the earlier record:** **N3** ("GD = residual boosting blocks") is **superseded** by *single frozen bulk + read-only namer* (boosting dropped, the P2.5 forward-leak wall); **S4** ("two GD organs") **collapses to one** (the namer; Interface-GD retired with the blocks); **S9** (fixed short-stack reader) is **extended** with the committed *head* (RanPAC/analytic), not just the placement. **Cost caveat → Phase 8:** RanPAC's projection proxy-cost is ~200× SLDA's — **SLDA is the cheaper within-noise no-gradient fallback** (passes the gate, more robust on depth-less inputs); cost was descriptive-only here, P8 prices it. → [`../src/phase7/README.md`](../src/phase7/README.md)

**S12 · The economy is drift-gated, and the gate is a *safety* mechanism — the two-brain loop is continual-safe run LIVE (Phase-8 close-out; RESOLVES S6, RESOLVES S11's cost caveat, EXTENDS S7, discharges the 80/20-proxy tag).** Both brains were run **live together** for the first time (SCFF learns forward-only on every input; the namer tracks the drifting taps via an awake gate + periodic sleep) on a CI+covariate-nuisance stream, on a new streaming **`partial_fit`** primitive (running Gram `(G,M)` + `λ_ema`, guarded ≡ a batch fit to 4e-15). **The committed economy:** deployed head **SLDA** · awake gate **DDM** on the namer's **error-EMA** (a two-threshold error detector; a class-direction tap-drift trigger validated but not shipped — DDM consumes an error rate) · sleep **grid-8 / full LUT history / λ_ema 1.0** · guard **cbrs** · envelope unchanged (GD reads taps, never writes SCFF). **The metered 80/20 is real, not a proxy:** with the gate on, the GD namer is **12.1% of total substrate energy** (GD-share 0.121 ≤ 0.25 on a behavioral ADC-centred meter); off, 77.8% (the gate *creates* the split). **OURS ≈ half the energy of BP+replay** at matched retention on the same substrate table (bp_ratio 0.501). **LIVE-SAFE:** the assembled loop holds worst-point (pre-sleep) BWT at **0.000** (0/5 seeds regress vs the known-boundary oracle) at oracle-level accuracy. **The crux (a new supporting decision):** **firing more forgets more** — always-pay (namer every step) forgets (−0.137) by chasing the recency-skewed stream, while the disciplined economy (rare gated fires + cbrs + full-LUT sleep) does not — **the gate is a safety mechanism, not just a cost saver.** **The spine, measured (not deployed at the gate):** the *validated* tap-drift signal fires on class **direction** (invariant to the nuisance covariate, 0.84×) not **magnitude** (the null spikes 10× and false-fires on 94% of nuisance steps — density≠class, 8th coat) — but the shipped gate rides the error-EMA. **Two Phase-8 corrections to the plan's intuition:** the gate's value is **safety, not just cost**; and **regular cadence beats boundary-aligned sleep** (grid worst-BWT 0.000 vs oracle-boundary −0.439, because the worst point falls mid-segment). **Consequences for the earlier record:** **S6** (threshold-gated learning) **resolves** to DDM on the namer's error-EMA (the class-direction tap-drift trigger validated but not shipped); **S11's cost caveat resolves** to **commit SLDA** (metered 69× cheaper namer, AA ties/beats live — RanPAC kept as the accuracy/spine reference, not deployed); **S7** (sleep) is **extended** to the grid-8 / full-history / λ_ema-1.0 detector-driven cadence (was oracle-boundary). **Still owed → Phase 9:** the fair same-budget **BP+replay *accuracy*** baseline (the existential test; P8 settled *energy*, not accuracy), natural multi-class **A5**, the read-side noise residual, and re-tuning the (drift-rate-conditional) cadence if **N2** slows the drift. Behavioral simulation only; no SPICE. → [`../src/phase8/README.md`](../src/phase8/README.md)

**S13 · The lifelong maintenance loop is FROZEN — the bulk rotates (it does not forget), four knobs keep the committed loop, and the freeze re-confirmed the sleep cadence (Phase-9 close-out; RESOLVES N2, MEASURES the bulk-drift assumption, EXTENDS S7, adds eviction=CBRS, resolves the read-side residual, LOCKS the object).** Phase 9 tuned the five still-open knobs of the maintenance loop *around* the frozen head against **internal signals only** (measured drift, BWT vs the known-boundary oracle, the metered economy — never the P10 BP+replay number) on a genuinely **lifelong** stream (many revisit cycles, so drift accumulates past P8's single pass), then **froze** the object at a commit hash for Phase 10 to race. **P9.0 — the founding assumption, MEASURED:** the SCFF bulk **rotates but does not forget** — an optimal probe *re-fit* on the current bulk holds ≥ its birth score on held-out early-task data throughout (destruction final 2.2×, min 0.966; Davari 2203.13381, rotation factored out) while a *fixed* head fit at birth rots to ~0.07. The "SCFF doesn't forget → sleep is cheap" caveat the whole architecture rests on is discharged; N2 is **not mandatory**. **P9.1 — N2 STRUCK** (standby → struck, the last open decision-record knob): no read-side arm (EMA-view / LLRD-rate) sleeps sparser or improves worst-BWT — the rotation-only drift is already tracked, so N2 has nothing to grip; EMA-view *worsens* BWT (train/eval frame mismatch); LLRD is honestly rate-only (early/mid taps unmoved — no Stage-1 reopen). **P9.2 — keep ALL-TAP consolidation** (S7 extended): truncated sleep re-fits are 12–52× cheaper but forget more under live lifelong drift (worst-BWT −0.511/−0.373 vs −0.317) — all-tap's capacity is the margin that absorbs the drift. **P9.3 — eviction = CBRS** (a new supporting decision): with a bounded evicting `StreamingLUT`, at a pressure-point cap the bound bites (no bounded policy matches the unbounded oracle — a property of the *cap*, the scaling law, not the policy), and CBRS is the **best-bounded** policy — it ties the herding **magnitude null** (a **buffer-spine null**: density ≈ class at the buffer here, so the control cannot fire) and decisively beats reservoir/recency (−0.400 vs −0.607/−0.707); the holding cap **grows with #classes** (a memory-scaling law). **P9.4 — read-side residual RESOLVED read-side:** the Phase-6 input-transducer *directional* residual really dents the committed SLDA loop (gate fires, dent +0.115, 5/5; worst seed retention 0.504), and **prototype re-anchoring** — re-forwarding the raw LUT through the current bulk under the shift, the plan's *own sleep mechanism*, direction-grounded, no covariance estimate — recovers retention 0.787 → **0.986**; the Phase-6 "scoped-YES → Stage-2 read-side" debt is discharged. **P9.5 — FROZEN.** The first assemble at the inherited P8 **grid-8** cadence (= the P8.6 shipped object bit-for-bit, since every P9 knob was struck/kept) **failed** the worst-point oracle-veto **2/5** on the lifelong revisit stream (sparse sleep → deep pre-sleep troughs on high-variance seeds, worst-BWT −0.517); the freeze-driven **cadence re-confirm** (the P8 "cadence is drift-rate-conditional" debt, owed by the freeze) found the fix is denser sleep — **grid-4** is the knee: worst-BWT **−0.028** (ties the oracle, **0/5** regress), AA 0.494 (= the shipped object), metered GD-share **0.178 ≤ 0.25**. The frozen loop is an **order of magnitude safer** at the worst point than the shipped grid-8 loop on the lifelong stream (−0.028 vs −0.317) — the P8 cadence, tuned on a single pass, silently under-consolidated under revisits. **Two Phase-9 corrections to intuition:** the live diagnosis that the worst-point gap was the committed gate's *fire-timing* (unfixable in P9, since the gate is committed) was overturned — denser **sleep** closed it, because frequent consolidation masks the DDM-vs-onset timing; and the read-side defense planned around an SLDA **covariance** re-estimate was overturned — **proto-reanchor** (no covariance) suffices. **The frozen object** = NoiseAugContrast bulk · SLDA · DDM-on-error-EMA gate (class-direction tap-drift validated, not deployed) · N2-struck · all-tap · CBRS · proto-reanchor · **grid-4** lifelong cadence · envelope unchanged (GD reads taps, never writes SCFF). **Consequences for the earlier record:** **N2** resolves (struck); **S7** extends again (all-tap depth + the grid-8→grid-4 lifelong cadence, the drift-conditionality discharged with a number); the bulk-drift assumption is **measured** (rotation-only). **Still owed → Phase 10** (the freeze consumed none of it): the fair same-budget **BP+replay *accuracy*** baseline (the existential test), natural multi-class **A5**, the multi-domain gauntlet, and the noise showcase on a **held-out** battery (P9.4 tuned only on the home residual). Behavioral simulation only; no SPICE. → [`../src/phase9/README.md`](../src/phase9/README.md)

**S14 · The founding bet, judged against a fair opponent, is REFINED not inflated — a substrate-native continual learner; the two halves banked separately (Phase-10 close-out / STAGE-2 CLOSE-OUT; DISCHARGES the existential BP+replay debt Phases 4/8/9 carried, CHARACTERIZES the SCFF:Namer ratio, CASHES the Phase-6 noise arc on the assembled object, CLOSES Stage 2).** The frozen object (S13) was raced — untouched — against a **fair, budgeted, tuned experience-replay** backprop learner (Prabhu CVPR'23: under a matched **FLOPs/sample + memory-bytes** budget ER is *strong*, not a strawman; tuned on a disjoint seed 7 ∉ the raced set, byte-matched to OURS's LUT) across the continual home, a 5-domain native domain-IL gauntlet, a held-out noise battery, and natural digits. The verdict shapes were pinned **BLIND** before any baseline number existed; the only dial moved was the declared **cadence cost axis**. The founding bet — *"an 80/20 forward-only continual learner that beats backprop's economics **and** accuracy"* — banks as **two halves, honestly separated (R4):** **ECONOMICS = substrate-realized, not algorithmic.** On the **same digital substrate** OURS's 12-layer unsupervised bulk costs **1.5× MORE** than a small tuned ER (P10.1 3.46e8 vs 2.25e8; P10.3 1.47×) — the 80/20 algorithm alone does **not** win the energy race against an efficient discriminative net (the honest **R1** outcome, pre-registered: OURS forwards more FLOPs/sample, 96 938 vs 65 268). The **chip (OURS-analog) is 3.4–3.5× cheaper** than that same GD model on a conventional digital accelerator — but that advantage is the **analog crossbar** (a *meter-structural floor*, near-free CIM MACs by construction of the behavioral meter), **not** the algorithm. "Less energy than modern GD" holds **for the chip**, honestly scoped as substrate-driven; on a same-substrate (accuracy × energy) Pareto a small tuned ER **dominates** OURS. **ACCURACY = competitive-on-home / trails-on-static / WINS-on-continual-safety.** OURS **ties** ER-strong on the hard synthetic home (0.494 vs 0.498, within δ) but a tuned ER **beats** it on recognizable **natural digits** by **+0.071** (0.950 vs 0.879, P10.5) — OURS is **not** a static-accuracy competitor (P4, re-confirmed; the "beats backprop accuracy" claim is *not supported* on final/static accuracy). OURS's real accuracy edge is continual **stability**: on the honest worst-pre-sleep read it is the **safest of the field** — worst-BWT **−0.028 vs ER's −0.272** (≈10× less forgetting, P10.1), worst-point all-prev retention **0.490 vs 0.350** with higher anytime accuracy (AAA 0.519 vs 0.433, P10.3), order-robust. **NOISE = a clean win** over the fair opponent: OURS-hardened **≫ BP+replay on every held-out channel** (iid 1.095 vs 0.608, directional 0.978 vs 0.225, adc3b 0.923 vs 0.300, nuisance 1.000 vs 0.469, P10.4) — the Phase-6 "scoped-YES" cashed on the whole object; a small directional/ADC residual (~0.02–0.08 > δ) is **named → the analog-realism (SPICE/PVT) layer** (the battery was re-parameterized, not structurally novel → "confirms P9.4," honestly downgraded from "payoff"). **New supporting decisions:** the model's declared operating-point family is a **5-cadence cost-frontier** `{4,5,6,8,16}` (grid-4 the committed headline, **never swapped**; grid-5 the δ-eligible Tier-1 showcase rep; grid-6 fails the δ-BWT gate at −0.087; Tier-2 {8,16} the legible degradation arms — energy monotone 6.70e7→3.99e7); and the **SCFF:Namer ratio is characterized** across difficulty (GD-share 0.107–0.178 ≤ 0.25, the gate rarely fires and the loop consolidates via sleep — was assumed ~0.12–0.18, confirmed). **Two Phase-10 outcomes the plan pre-registered as live and the sims confirmed:** the ~0.49-AA home meant the accuracy-loss branch was real (a budgeted ER edges/ties OURS — *reported*, not tuned away), and the same-substrate energy cut genuinely goes *against* OURS (the racer's shape is its own uncapped best choice — threat h, reported not hidden). **The Stage-2 close-out:** the neocortex brain — cheap unsupervised structure (SCFF) + a gradient-free continual-safe namer (SLDA) + a metered economy (DDM gate) + a frozen lifelong loop (grid-4) — is **done and validated honestly**: a substrate-native continual learner, safest and most noise-robust of the fair field, with a substrate-realized energy advantage over conventional GD, and a named residual + a static-accuracy gap flagged to future work. The founding bet is **refined, not inflated.** Behavioral simulation only; no SPICE. → [`../src/phase10/README.md`](../src/phase10/README.md)

**S15 · The limit map — the validated object taken to real data + scale, drawn honestly with its wins, losses, and floors (Phase-11 close-out; ANSWERS the three reviewer strikes, DECOMPOSES the architecture claim, MAPS the deployment envelope, EXTENDS the economy/substrate characterization to scale; re-opens NO frozen decision — Arm A bit-equal committed, Arm B a pre-registered scaling rule, NOTHING tuned in either arm; ER re-tuned per arena on the disjoint seed 7 so the asymmetry runs against the home team; verdict shapes pinned BLIND).** The red team's charge was "wins only on toy data." Phase 11 answers it as a **LIMIT-MAP** — 8 arenas × 5 capability channels, every cell win / tie / loss / FLOOR with its number ([`../src/phase11/exp9/figs_p11_9/LIMIT_MAP.png`](../src/phase11/exp9/figs_p11_9/LIMIT_MAP.png)) — not a highlight reel. **Strike 1, "isn't this just SLDA?" (P11.1 decomposition):** Δbulk = (bulk→namer) − (proj→namer) tracks **arena nonlinearity** — **+0.417** on the nonlinear synthetic home (0.172→0.589, and the bulk beats a random 12-layer reservoir 0.589 vs 0.389 → *learned* structure, not just depth), +0.008 on MNIST-40, **−0.014** on near-linear digits — while the continual **safety** (worst-BWT ≈ 0) is the **closed-form namer + gate + sleep, not the bulk** (proj→namer forgets no more than bulk→namer). So the two organs decompose cleanly: *the SCFF bulk is the nonlinear feature learner; the safety is the SLDA-plus-gate-plus-sleep machinery* — **NARROWED-with-mechanism**, an honest and precise pitch answer (kill-criterion (i) did not fire). **Strike 2, "toy data" (P11.2–P11.4):** on nature's own sensor-aging drift (**gas**, Vergara 2012) the frozen recipe is the **strongest online learner in the room** — prequential balanced accuracy **0.789 ≥ a per-arena-tuned ER 0.756**, beating persistence by +0.184 (Arm B native-128 → 0.856); real MNIST holds the bet (safety worst-BWT −0.012/−0.046 vs ER −0.196/−0.162, order-invariance |Δ| 0.003/0.011, static AA trails — continual-not-static, Arm B scales); the cross-dataset 30-way gauntlet (MNIST→Fashion→CIFAR-gray) is **order-invariant across data *types*** (|Δ| 0.007/0.004) with all three types alive and **TYPE-ROBUST when scaled** (Arm B worst-ret 0.581 ≥ ER 0.551). The honest floors: **HAR / electricity / covertype FLOOR** — the ELEC2 label-autocorrelation trap (Souza 2020), where *no* learner beats the no-change baseline and the field leads OURS by ~0.07 (two-sided grey cells) — the map separates **drift-difficulty** (gas: real, OURS wins) from **data-difficulty** (autocorrelated labels: persistence wins, everyone floors) by measurement. **Strike 3, "does it scale?" (P11.5–P11.7):** the bench-**pinned GD-share-vs-W shape is CONFIRMED at the run** (measured [0.17,0.28,0.45] tracks the meter-derived [0.21,0.34,0.53] — the economy does *not* improve with width, the SLDA solve ~O((L·W)³) is the term that scales worst, refuting the temp2 direction-guess at the meter source); capacity **buys the accuracy gap back** (0.42→0.50) at that rising economy cost; the **analog substrate factor GROWS** with width (**5.4→7.4×** — the crossbar prices extra bulk MACs near-free, so the compute-in-memory edge *widens* at scale, the chip's best sentence); the class-count **retention crossover is real by C=20** (a byte-matched replay buffer dilutes to ≈0 retention 0.014 while the gradient-free prototype+Gram namer holds 0.233 — per-class fidelity is C-independent; the honest counterweight is that the all-tap F×F covariance is a large fixed *byte* cost); and on gas the retention-tuned ER costs more FLOPs/sample than the frozen recipe → a **throughput regime win** (arena/opponent-dependent — it inverts P10's synthetic-home FLOP inversion, reported as such). Fashion (r2) holds the safety bet; **CIFAR-gray (r3) FLOORs** at its resolution (joint ceiling 0.199). **The honest deployment envelope:** a substrate-native continual learner that wins **safety, order-invariance, noise-robustness, real information-bearing drift, and every scaling read of the economy/substrate**, while **trailing on static accuracy** (continual-not-static, P4/P10.5 at scale) and **flooring** where the data is at its resolution or label-persistence limit. **Owed/named (never faked):** the raw-784 Split-MNIST anchor validation (P11.2 ran it at porthole-40 — a config note, not an ER failure); the D=160 cross-dataset Arm B (descoped to D=80 pending the F5 einsum→GEMM apparatus fix); P11.8 the features arena (no off-box precompute window); Yearbook/BloodMNIST/CIFAR-100 (openml API down this box). **The author's bar, met:** *"if everything fails, let them attack my math, not my missing evidence"* — the map ships with its red and grey cells. **→ the analog-realism (SPICE/PVT) layer** inherits the named residual + the directional/ADC reads; beyond the numbered phases, the recurrent lifelong brain remains the north star. Behavioral simulation only; no SPICE. → [`../src/phase11/README.md`](../src/phase11/README.md)

---

## The drift fix (resolved)

GD breaks if SCFF drifts faster than GD can re-track. This is **not** a contested-weight problem — taps mean SCFF and GD never share a weight. So we bound drift cheaply with the **plasticity gradient** (N2). The budget rule: lower the read-layer learning rate (free) rather than fire GD more often (burns the 80/20). Permutation / re-clustering events are the one thing slow-LR only *delays* → guard with BCM homeostasis (on the existing momentum register) + the multi-layer tap as redundancy.

---

## Open knobs — the sims decide, not us

- Front:back plasticity ratio — exp 2.1 (frozen vs slow vs fast read-layers).
- Gate threshold — absolute vs plateau-slope.
- Sleep cadence — couples to cluster-churn rate, and to EMA τ if the EMA-view is used.
- How little history sleep needs (3.x); LUT vigilance threshold.
- Margin loss vs log-loss; two-sided vs pure-contrast objective.
- How many chained blocks before direction-chaining strains.
- Tied-from-start vs converge-to-tied first layer — does forcing summation cost any training ease?

---

## Status

**✅ Phase 1 ran and is complete (2026-06-20, exp0 → exp4).** Spine confirmed; numbers found. Full synthesis: [`../src/phase1/README.md`](../src/phase1/README.md) + ledger [`../src/phase1/RESULTS.md`](../src/phase1/RESULTS.md). **Verdict:** a cheap, *forgetting-robust* **continual** learner — one block generalizes better than backprop (smaller memorization gap) at ~10% backward cost, and in the continual regime sleep recovers what online backprop catastrophically forgets; it is **not** a deep static-accuracy competitor (SCFF is weak, shallow-is-better, depth saturates). **Knobs the sims set:** goodness = **sum ‖h‖²**, θ=2.0, input-norm, **tap all SCFF layers** (S3 "last n" corrected), two-sided loss, **full residual ε=1** (N3 boosting; Ch9 delta off), **slow read-layers** (N2 ρ≈0.3), default **H=64**. **✅ Phase 2 (depth) also ran and is complete (2026-06-21, P2.0 → P2.6).** Synthesis: [`../src/phase2/README.md`](../src/phase2/README.md) + ledger [`../src/phase2/RESULTS.md`](../src/phase2/RESULTS.md). **Verdict: depth is not SCFF's lever** — a deep SCFF stack cannot earn depth on *either* axis: not **transmission** (P2.1 — norm × goodness fixes the mechanism but not the depth curve) and not **objective** (P2.2 — *even a perfect label-oracle* hard negative doesn't bend the slope). The wall is **intrinsic to forward-only locality** (no cross-layer coordination — what GD exists to supply). Depth instead comes from **boosted ensembles of *shallow* SCFF blocks with tiny GD readouts** (P2.5 — `read`/ensemble earns it cheaply: ~85% of GD accuracy at ~1/6 backward; `write`/re-inject **fails**), and the recipe **preserves the continual win + is continual-safe by construction** (P2.6 veto). So the *SCFF-likes-width / Scap-likes-depth* collision resolves by the **other horn — depth = block-count, not SCFF-layer-count**. **Tools' fates:** online batch-norm/SymBa/hard-negatives = *don't earn depth* (mechanism-only); the γ scalar + deep supervision (P2.3/P2.4) = *skipped, moot*; **GD-between blocks = the survivor**. **Spec set:** recipe `[SCFF×k → GD-readout]×N` (**read**, healthy *layer-norm + linear + contrast* cell, sleep-consolidated, few blocks); **contrast > two-sided θ**. **✅ Phase 3 (depth round 2 — the objective reframe) ran and is complete (2026-06-22, P3.0 → P3.3).** Synthesis: [`../src/phase3/README.md`](../src/phase3/README.md); survey: [`../research/papers/README.md`](../research/papers/README.md). **Verdict:** the depth wall is intrinsic to the **energy objective `Σh²`**, not to forward-only locality — and the fix is a **discriminative** objective + the user's cross-layer coordination. *(Note: the pre-run design guessed masked-reconstruction as primary; the sims overturned it — reconstruction preserves **density** (below random), while **contrast** preserves **class** (above random, growing with depth). So the **adopted recipe = `[contrast (InfoNCE, two-mask) + coordination window w≥2] SCFF bulk + sleep-consolidated readout`** — it SUPERSEDES energy-goodness `Σh²`; reconstruction was rejected.)* It re-earns + improves the continual win (P3.3 veto: BWT −0.017 vs energy −0.026). **✅ Phase 4 (characterization — the capability map) ran and is complete (2026-06-22, P4.0 → P4.7).** Synthesis: [`../src/phase4/README.md`](../src/phase4/README.md) + ledger [`../src/phase4/RESULTS.md`](../src/phase4/RESULTS.md) + `figs_summary/CAPABILITY_MAP.png`. **Verdict: a substrate-native *continual* learner, not a static-accuracy competitor** — a gap-to-genuinely-tuned-BP map across 7 axes: **WINS** A6 continual (decisive, robust across difficulty), A2 nuisance-dim (crosses *above* BP in high-D; Mono collapses), A3 depth-composition (generalizes, representation-level), A4 depth-is-cheap (the **80/20 cost advantage is depth-gated** — flat-in-depth for OURS, linear for BP); **TRAILS** A1 difficulty / A5 class-count (capture is the read; real data far kinder than synthetic); **honest NEGATIVE** A7 eval-time noise (the per-sample-layernorm nuisance-robustness↔noise-sensitivity tradeoff; train-with-noise untested). **✅ Phase 5 (SCFF close-out — the depth-readout fix) ran and is complete (2026-06-30, P5.0 → P5.9).** Synthesis: [`../src/phase5/README.md`](../src/phase5/README.md) + ledger [`../src/phase5/RESULTS.md`](../src/phase5/RESULTS.md). **Verdict: the depth decay is SOLVED (scoped) — it was *objective-locality*, not an intrinsic Tunnel** (full-credit w12 composes the whole 12-layer stack, so depth is *curable*, not walled). Two *free* levers fix it: a **sharper InfoNCE temperature (0.2)** earns the depth back — the spine, again: it keeps each update on the class *direction* (an lr-matched control shows ~82% of the lift is direction, not learning-rate), so the re-tuned **readout beats tuned-BP** on the headroom task (0.550 vs 0.531) and the probe tail reaches the w12 ceiling (0.530→0.562 w4 vs w12 0.556) — and **per-depth heads read by a short fixed stack** read the continual home **8× cheaper** than all-tap (0.547@9.0k vs all-tap@72.5k). Continual-safe (P5.7: temp0.2 BWT −0.026 vs −0.017, the A6 sleep-recovery mechanism intact) and real-data-confirmed (P5.8: decay real on digits + CIFAR-flat; temp-fix +0.152 on digits, null-but-safe on the no-headroom CIFAR-flat wall). **Struck (failures are data):** the adaptive max-softmax early-exit (P5.5 — the flat continual home rewards *pooling*, not per-sample placement; confidence is a poor depth-selector) and the frozen-residual preservation cell (P5.6 — skipped; the cheaper levers already closed the gap the fixed reader doesn't read). **The committed cell: `SCFFContrastOverlap` temp0.2 / w2, L12 bulk, NO residual, fixed-reader deploy** (truncate ~L2–3 on the continual home; all-tap for peak accuracy; **w4** = bounded depth-closer for compositional tasks). **The cheap brain is finished.** **✅ Phase 6 (noise-robust SCFF — a Stage-1 *extension*) ran and is complete (2026-07-01, P6.0 → P6.8).** Synthesis: [`../src/phase6/README.md`](../src/phase6/README.md) + ledger [`../src/phase6/RESULTS.md`](../src/phase6/RESULTS.md). **Verdict: Scoped-YES — A7 (eval-time noise) is real, OURS-specific & directional** (0.60 vs a linear readout 0.96, 5/5; reproduced on synthetic + digits + CIFAR-flat) and it lands on the **SCFF objective**, not the GD readout (LP-FT). The fix (**S10**) — generic noise-augmentation — **substantially but partially** hardens the dominant tap channel (0.82→0.87, 5/5 paired; near not above the 0.90 band), **improves** clean acc, **passes the A6 continual gate** (BWT −0.022→−0.017), and holds on natural data; the **input-transducer residual** → Stage-2. **Not NO** (the objective did not need reopening); the norm was left intact (P6.3 STOP ② — it is load-bearing for depth + A2). **✅ Phase 7 (the GD namer / readout — Stage 2's first phase) ran and is complete (2026-07-02, P7.0 → P7.6).** Synthesis: [`../src/phase7/README.md`](../src/phase7/README.md) + ledger [`../src/phase7/RESULTS.md`](../src/phase7/RESULTS.md). **Verdict (S11): the 20% is NOT gradient descent** — the committed namer is **RanPAC** (closed-form random-projection + running-Gram ridge prototype) **+ a class-balanced-reservoir guard**: it ties the gradient MLP anchor on accuracy×BWT (3-way tie with SLDA), leads on natural digits (#1, 0.949), passes the A6 gate, and is the most spine-clean of the tied cluster. **The spine bends** (magnitude-wins-spine-bends, Δ=0.128 synthetic → −0.036 digits → ≈0 CIFAR-flat: the direction-pure cosine is spine-clean but sub-competitive where the bulk has structure). Sims overturned two guesses: the imbalance guard is **cbrs, not AIR**; the "multimodality cliff" is **anisotropy (a tied covariance), not multimodality**. N3/S4/S9 revised (see S11). **Cost → Phase 8** (RanPAC ~200× SLDA; SLDA the cheaper no-grad fallback). **✅ Phase 8 (the economy gate + the cost meter — Stage 2's second phase) ran and is complete (2026-07-02, P8.0 → P8.6).** Synthesis: [`../src/phase8/README.md`](../src/phase8/README.md) + ledger [`../src/phase8/RESULTS.md`](../src/phase8/RESULTS.md). **Verdict (S12): the two-brain economy is real, cheaper *and* safer, run LIVE** — both brains live together for the first time (new streaming `partial_fit` primitive), gated by a **DDM** awake gate on the namer's **error-EMA** (a class-direction tap-drift trigger validated but not shipped) with a **grid-8/full-history** sleep and **SLDA** deployed (metered 69× cheaper than RanPAC → resolves S11's cost caveat). The metered 80/20 is **real** (GD-share 0.121 ≤ 0.25; the gate *creates* it — off it is 0.778); OURS ≈ **half** the energy of BP+replay at matched retention (bp_ratio 0.501); and it is **LIVE-SAFE** (worst-point BWT 0.000, 0/5 regress vs oracle). The crux: **firing more forgets more** (always-pay −0.137) — the gate is a *safety* mechanism. Spine measured, not deployed at the gate (the validated tap-drift signal is direction-invariant to nuisance, the magnitude null spikes 10× — but the shipped gate rides the error-EMA). **What's next → Phase 9 = the maintenance loop + owed baselines** — the fair same-budget **BP+replay *accuracy*** baseline (the existential test; P8 settled *energy*, not accuracy), natural multi-class **A5**, the read-side noise residual, the readout-aware cadence, and re-tuning the drift-rate-conditional cadence if **N2** slows the drift — map [`../src/stage2-design.md`](../src/stage2-design.md). The **recurrent lifelong brain is the north star**, beyond the numbered phases. Analog/PVT realism runs in parallel once a phase's ideal converges. **✅ Phase 9 (close & *freeze* the maintenance loop — Stage 2's third phase) ran and is complete (2026-07-02, P9.0 → P9.5).** Synthesis: [`../src/phase9/README.md`](../src/phase9/README.md) + ledger [`../src/phase9/RESULTS.md`](../src/phase9/RESULTS.md). **Verdict (S13): the maintenance loop is FROZEN, and the freeze made it safer than what P8 shipped** — the bulk **rotates but does not forget** (P9.0, the founding assumption measured, N2 *not mandatory*), four of five open knobs kept the committed loop (**N2 struck**, depth **all-tap**, eviction **CBRS**, read-side residual defended by **proto-reanchor**), and the freeze itself caught the one real gap: the P8 **grid-8** sleep cadence, tuned on a single pass, was too sparse for a lifelong revisit stream (worst-BWT −0.317, failed the oracle-veto 2/5). The freeze-driven cadence re-confirm → **grid-4** restores near-flat continual-safety (worst-BWT **−0.028**, ties the oracle 0/5, at AA 0.494 and metered GD-share 0.178 ≤ 0.25). The complete committed neocortex loop is **locked at a commit hash** for Phase 10 to race. **What's next → Phase 10 = validation / showcase** — the fair same-budget **BP+replay *accuracy*** baseline (the existential test), natural multi-class **A5**, the multi-domain adaptive gauntlet, and the noise showcase (a held-out battery) → an honest **Pareto** verdict + the Stage-2 close-out. **Freeze in P9, judge in P10.** **✅ Phase 10 (validation / showcase — Stage 2's fourth and LAST phase) ran and is complete (2026-07-03, P10.0 → P10.6, all 14 guards bit-exact; grid-4 reproduced bit-for-bit vs the P9 freeze).** Synthesis: [`../src/phase10/README.md`](../src/phase10/README.md) + ledger [`../src/phase10/RESULTS.md`](../src/phase10/RESULTS.md) + report [`../src/phase10/phase10-report.md`](../src/phase10/phase10-report.md). **Verdict (S14): the founding bet is REFINED not inflated — a substrate-native continual learner, banked as two halves.** Against a fair, budgeted, tuned ER: **accuracy** ties on the hard home (0.494 vs 0.498) but trails on natural digits (0.879 vs 0.950) — OURS is a *continual*, not static-accuracy, learner (P4 re-confirmed) — while winning **continual safety** decisively (worst-BWT −0.028 vs −0.272, ≈10× safer; gauntlet worst-point retention 0.490 vs 0.350) and **noise** on every held-out channel (0.92–1.10 vs BP's 0.23–0.61). **Economics** is **substrate-realized** (the 80/20 algorithm is 1.5× *more* than a small tuned ER same-substrate — the honest R1; the chip is 3.4× cheaper via the analog crossbar floor). On an (acc × energy) Pareto a small tuned ER dominates OURS; OURS's wins live off that axis (safety, noise, substrate). The cadence cost-frontier `{4,5,6,8,16}` and the SCFF:Namer ratio (0.107–0.178) are characterized; a small directional/ADC residual is **named → the analog-realism layer**. **Stage 2 is DONE** — the neocortex brain (cheap unsupervised structure + a gradient-free continual-safe namer + a metered economy + a frozen lifelong loop) is validated honestly. **✅ Phase 11 (the limit map — real data + scale, the red-team answer) ran and is complete (2026-07-05, P11.0 → P11.9; committed core + all three scaling extensions).** Synthesis: [`../src/phase11/README.md`](../src/phase11/README.md) + ledger [`../src/phase11/RESULTS.md`](../src/phase11/RESULTS.md) + report [`../src/phase11/phase11-report.md`](../src/phase11/phase11-report.md). **Verdict (S15): the LIMIT-MAP — the validated object across 8 real arenas × 5 capability channels, wins/losses/floors all shipped.** The three reviewer strikes answered: (1) the **decomposition** (Δbulk +0.417 nonlinear-home / ≈0 near-linear, beats a random reservoir) shows the SCFF bulk is the nonlinear learner and the safety is the closed-form namer+gate+sleep — "is it just SLDA?" answered precisely (NARROWED-with-mechanism); (2) on **real data** the frozen recipe WINS nature's gas drift (0.789 ≥ tuned ER 0.756, beats persistence +0.184), holds MNIST safety/order-invariance, is cross-dataset type-robust — and honestly FLOORS on the autocorrelated streams (HAR/electric/covtype, the ELEC2 trap, field +0.07); (3) it **scales** — the pinned GD-share economy shape confirmed, the analog substrate factor grows 5.4→7.4× with width, the prototype+Gram namer out-retains byte-matched replay by C=20, gas throughput regime win — while static accuracy trails and CIFAR-gray floors. Nothing tuned (Arm A bit-equal committed, Arm B a pre-registered scaling rule); ER re-tuned per arena; verdict shapes pinned blind. Next: the analog-realism (SPICE/PVT) layer; beyond the numbered phases, the recurrent lifelong brain remains the north star. Behavioral simulation only; no SPICE.

_Original next-action (now done):_ the experiment ladder in [ideas1.md](ideas1.md) (§ "The experiment ladder"), starting at **1.0 — full SCFF**, with mono-forward dual-rail and mandatory inter-layer normalization in from the first run.

The codeable Phase-1 spec is [`../src/phase1/README.md`](../src/phase1/README.md) (0th probe → Exp 1–3, with Exp 4 = gate + sleep). **Note the reorder:** Phase 1 runs chaining (Exp 3 ≈ ladder 4.x) *before* the maintenance layer (Exp 4 ≈ ladder 3.x sleep + Ch 7 gate) — structure first, maintenance once Exp-3 drift shows how much is needed. **Resolution (Ch 9 vs N3):** Exp 3 makes **N3 residual boosting** the primary chaining mechanism; the Ch 9 linear inter-block delta is demoted to an optional, off-by-default top-up. **Phase-1 pass metric:** held-out generalization is the headline; the **train−held-out memorization gap** is the block-vs-GD comparison (not the online curve, not a single scalar).
