# Background

First I have now ideas about it. I just love in math, physic, biology, the origin of life, the origin of planet, the origin of intelligence. I'm so passionate in 'how it works'. I've proven all linear regression for both ml and dl without even use matrix, cause I want to know how the intelligent live in. Then I went forward to hessain. And then I prove the double descend myself without any paper, And just see a double descend is just a soft regression flow of entrophy to stable place. I don't prove it by math, but prove it by matrix. From the first I use nD and m order taylor series to prove it, it's too muhc. SO I move back to some thing that fix, it's a real neural network hierachy. Then I just that each neural just have a weight as a storage of data, each output feature will be save it data into it. Making the specific path. The origin of intelligence is from random value. If every neural start first with no activate function open, eventually the loss function will reduce the weight from the lowest neural to hit it. Then the more we go, the more activate function allocate each neural storage. With soft noise, eventually, these noise will cancel itself, making the optimum of answer.

And then when I can see it... I'm not interest in ml or dl anymore. It's not real intelligence, it just the pattern recognize by brute force. LSTM is just time series remembering input. Transformer is just a bruteforce n\*n grid to match with context, even LSTM is more like intelligence than it. Trasformer in LSTM-form + chain of thought context is just a intelligence calling from a normal pattern. It can do only pattern it have train, just it break the context overload. Eventually it can't break the experience, even it can run to do something, but eventually it so lack of creativity. Even a opus 4.7 is just a smarl child that have a pros that so stable that can run 10hrs, it just gradually shape its output to make it smart, not smart at first due to normal base selection that is the backbone of algorithm.

So that time I move to something that more interesting for me, that is biologycell + ml, but... The more I search it, the more found that I can't acess it. I search for all 5 kingdom for all philum, and I see that those nerve cell are in only a animal.... Other species don't have any of this fast thinking intelligence. And to access it... it's so annoying and so hard for me. That time I'm so diappointment in this field. Making I'm going to the field I didn't think before. I go to play at circuit field in my electronic circuit class. I do: ChronoForge is a pure-hardware 2D game engine built on the Xilinx Basys3 FPGA (Artix-7) that runs with no CPU, OS, or instruction set — just raw event-driven circuits. It drives gameplay through three independent hardware timelines (Attack, Platform, Character) that process game objects in parallel, achieving 640×480 @ 60Hz within 18k LUTs and 7k flip-flops. Developers write games entirely in Python classes, which get compiled through a pipeline (Python → JSON → hex .mem files) loaded directly into FPGA ROM. The engine supports configurable object pools for triggers, colliders, and characters, with player input handled via four onboard directional buttons.

After than that so big self-architecture, I'm so tire and burnout about mount that just wake up and play game. Then... I got the super fucking shit ideas..... If I can't acess real biology cell.. What if... What if I build my self biology cell with analog cpu? With this Ideas, I spend about 7 mounts now to build my all intuintion myself, and desgin all of this.

TBH, my qoute is "The fast answer will destroy your creativity", so this all document, I build everything without even done to read how real axon look like, how real neurosceince is, how other company on the world handle with it.

# Ideas

Brain is like LSTM more than Transformer. It's a recurence between prefontal cortex that think and try to imagine the past memory, then hippocampus serve data for prefontal cortex.

The "Understand", "fell makesense", "true", etc. that we use today in science isn't a universe truth. It's just a type of feeling that our past experience connect it. Such as I try to figure out is this car color orange or not, at first I don't see the orange color and said "make sense". But first I see the car color, Then I try to think in my memory "which this color look like", then my memory serve orange fruit and apple fruit, And then I remember that orage fruit is orage color, and car color is like orage. So then car is orange, "make sense".

With this Ideas, I'm more passionate in it. So then I go to see how real brain communicate this path between prefontal cortex and hippocampus. Then I see about synapse move in 3D with hormone. There just doesn't exist only the axon multiply signal level. Now some hormone in positive feed back can move the tail of synapse to more close too. And moreover, there are a furior sum of signal at the axon too. Each Axon didn't only recieve a stable analog level signal, but at the axon, it recieve a pulse from many synapse to pass the threadshould. And more and more over, there have numbness feature too, the more signal it have, the more it less response to it. Don't count even that real neural can be destroy and create anymore, or even it can shift it sin/cos wave itself... I'm so excite with it all.

So then I try to apply it all to my analog circuit. I found some shit problem.... That is... The biologycell is so fucking genius. In analog cpu, one wire can just only send a 1 signal level. But real biology gap can transparent multihormone at multilevel with 3D axis free... So now I see... I see that I may able to adapt those real biology cell inspire the encoding analog circuit stucture. I may able to move our discrete math now to continous with efficient of compataion. (The real biology cell machanism should write with some that that actually continounous) That is a core ideas of this project.

Finally, I'm go back to binary system computer architecture of von numann, I try to figure out so long, how will I fix it. But... I see.. The problme is the architecture itself. I don't mean that it poor. Just a repeating of instuction isn't good for some continounous compute. Don't count even that we wasting most of our space for just SRAM to keep brach history to do brach prediction. It's so inefficient to compute basic thing but huge. Even GPU found the same problem, now we expand the traffic size, but eventually we still require the CPU to asssemble those bruteforce calculation.

From this all ideas, So I decide that what if... What if I use real brain encrytion neural machanism to compress the storage I have to hold? What if I don't have to LDR or STR any answer to memory anymore? Just a cpu that recive input and predict output. Compute, Backpopagation, and learn everything inside?...

What if I use whol analog capacitor to hold meaning without changing it back to any binary (except only when we save weight to shutdown whole board), what if all SRAM space that we use to keep brach prediction before, Use to keep capacitor to hold data complexity? What if we make Main ALU that using SRAM to wiring Biological path way to compute from scatter cap in all cpu space? What if we build our op amp ourself and use it natural behavior without changing back to human maht?.... From this all, it's the time this project born.

# Aiming

Making encrytion algorithm inspired by real-world biology cell. Experiment to copy real cell mechanism into the analog circuit. Aim to the complexity encription, Prove the strong adventage of this architecture that is "Need less computation process in backpapagation on concept cells that fire together wire together", "Pull out maximum neural power, The efficient neural network, using less capacitor or SRAM, but still have much model complexity (Lottery Ticket Hypothesis)", "Computation speed that using analog type with no translate back to save, Using real natural behvior analog circuit without change back to normal math", and "Don't use any LDR/STR to cut model apart, Everything will be in CPU only, except input/output. (When intial board and shutdown board will be the time we log out all capacitor weight only)"

Then to makesure that this project will come true, I have to limit our scope fist. Now, I touch varios of hardest field in the world at once. So now this project will be only python simulation + math prove about my architecture decision. Such as why I chose to select A from real cell, not B? How it experiment result said? Is that module is enough? etc. Building the huge one, then gradually refined to super efficient architecture. At first everything will build with no clue (my imagine). But at last, everything should be minimal physical intelligence system. And even now we will build only python and math experiment, but all architecture ideas should be able to do in real analog circuit too. Such as we know that we can use cascode + refill circuit to use capacitor with less leakage, so now we can use capacitor to keep dynamic weight and use it machanism in our theory. It's possible. But the part that we don't sure yet, can we build the real circuit or not, we will not use it at our main path yet, until we found the little possible of it.

Summarize, I will build non-von numann cpu that capacitor will scatter around the cpu board, we will recapture the space that weight for brach prediction. Then We will use SRAM + ALU to access those capacitor and wiring the network. It's a SRAM for wiring path, sending meta data, keeping sigh, define attribuite, etc. While Capcitor is main weight computation, keep only multi rangesignal in 1 line. Building the huge one first, then gradually refined to super efficient architecture for minimal physical intelligence system.

# Core Ideas Chain

1. The Directional Learning Principle
   "If wrong is left, correct is right — eventually you get intelligence."

Not discrete backprop steps. A continuous error signal that self-organizes direction.
Like water finding the lowest point, not gradient descent iterating.
Binary path reinforcement: did this routing path contribute to correct output or not.

2. The Discrete Computation Critique
   "Real nerves use continuous, not discrete. Intelligence by NAND gate is wrong for computation."

Digital neural nets are a metaphor mistaken for the actual thing.
Real neurons: membrane potentials, refractory periods, frequency encoding, timing relationships.
Discretization throws away the actual computation.
The dot product was designed for digital silicon. Wrong primitive for analog.

3. The Capacity Allocation Insight
   "Each neuron has allocated space for each output. Lottery Ticket Hypothesis shows
   we don't use full potential of each neuron."

Sparse subnetworks already exist inside dense ones (Lottery Ticket).
What if each neuron intentionally allocates its continuous capacity across
output dimensions instead of accidentally?

4. The Recurrent Memory Loop
   "Prefrontal sees memory from hippocampus, calls more related memory,
   hippocampus receives and updates, prefrontal thinks again."

Working memory = a conversation, not a database lookup.
Iterative, recurrent, context-dependent retrieval.
"The right feeling is just the pattern of feeling from past memory."

# Operator

- Description
  - The main math operator for ALU

- Basic Math For Digital
  - Using same standard

- Basic Math For Analog
  - Plus/Minus
    - Using op-amp liked circuit that play with VCC and GND range
    - Because using low range, so it need less gain

  - Multiplication
    - Using op-amp liked circuit that play with load
    - Because using super high range for R>1, need super high gain

- Capacitor Charge
  - Using cascode circuit to use current mirror with low drain
  - Using 8 bit level SRAM to refill capacitor on clk

# The Nerve Cord Network (NCN)

- Describtion:
  - The main Element to compute. Having ALU hard wired that already connect everything. It's the lowest compute core for this architecture. Using 2-3-3-2 neural network with Simple Linear Regression + Relu to keep neural memory
  - The Lowest ALU will be the side of this NCN
  - Forward: Input (capacitor + coscode current mirror) -> hardwire 2-3-3-2 network -> 2 Output (Capacitor charge)
  - Backpopagation: normally we will use distribution share (output _ previous layer weight _ this layer weight), But this simple NCN, we will use constant distribution. the activate path (not close by relu) will be reduce the weight by c equal for all layer. (Easy to track)

- Layer:
  - 2 input layer (input cap, cascode current mirror)
  - 3 hidden layer (hardwire opamp)
  - 3 hiddle layer (hardwire opamp)
  - 2 output layer (output cap, charging)

- Formular:
  - Simple Linear Regression
  - y = aW + b
  - a = relu(y)

- Material:
  - scap (SRAM + Capacitor)
    - 1 SRAM to keep history state (this neural open or not in this computataion)
    - 1 wire for update_signal
    - 1 bits SRAM to keep a sign
    - 1 Capacitor + Coscode circuit to keep it analog value
    - 8 bit level SRAM to refill capacitor on clk
  - 3\*2 (w) + 3 (b) scap for L1 to L2
  - 3\*3 (w) + 3 (b) scap for L2 to L3
  - 3\*2 (w) + 2 (b) scap for L3 to L4
- ALU:
  - Operation
    - relu use Super Diode Opamp
    - multiplication use Multiply Op amp
    - Addition use Add Opamp
  - Material:
    - 3\*2 (mul) + 3 (add) + 3 (relu)
    - 3\*3 (mul) + 3 (add) + 3 (relu)
    - 3\*2 (mul) + 2 (add) + 0 (relu, last neural)
- Back Propagation:
  - concept:
    - Set input
    - Set wire update_signal to 1
    - Using tri-ode to send feedback (minus, zero, plus)
    - The scap check it history bit
    - Each scap will have sign SRAM that will use with this possitive/negative gradient
    - Optional\*\*, we can use time to apply distibution to make each neural/layer have real distribute share, but it's may not stable now, require optimization otpimize.

  - ideas:
    - even we use constant to update capacitor, but real capacitor charge by e^t. So this constant open VCC will make exponential update. And we get likely possitive feedaback, the more contribute neural, get more feedback, get more weight increase, pull out all distribute, so weak neural get more weak. But with this e^x formular, will have stable trend at the center (the more weak, it hard to increase. the much stron, it easy to reduce.)

  - Flow:
    - input open
    - ALU compute
    - output charge
    - the output from layer 1 go to input for layer 2, recursive

# The Nueral Brain Network (NBN)

- recap:
  - The previos part we have NCN (Nerve Cord Network) that have 2 input and 2 output, which inside can keep its complexity as a lowest unit of compute

- Concept:
  - We will built the NCN network.
  - 2 input -> 4 NCN-input -> 2 NCN network -> 4 NCN-output -> 2 output
  - 2 previous output -> 2 NCN-input -> NCN network -> 2 NCN output

- ALU:
  - 1: high cost:
    - We will using 4-8 NCN ALU framework that can run the group of NBN at once
  - 2: low cost
    - we will using 1 NCN and gradually build NCN by NCN, keeping it result at capacitor

- ideas:
  - In the real neural network that have multi output, each neural will have allocate weight to compute it. Such as Nerual2 at Layer3 will have strong allocate for feature A, while this same neural will have weak allocate for feature B, making specific nueral path for classified A and B.
  - With this idea we will reuse the layer, Normal: L1 → L2 → L3 → L4 → L (each layer used once, linear)
  - This model: L1 (generalist) → L2 (specialist) → L1 (generalist) → L3 (specialist) → L1 (generalist) → L4 (specialist)
  - The L1 neural will be use for redundancy, making model complexity increase while using same neural amount. If we have great balance of L1, we can use its 100% allocate space on the Lottery Ticket Hypothesis.
  - Then L2, L3, and L4 will get context drive the generalist L1. Like L2 have ideas for feature A, then L1 go to expand it detial, then L3 get it and analyze it again. Making a real complex activity path way.
  - The specialist NCN will be set context

- limit:
  - So eventually, our ALU will have limit size, and reuse layer ideas required some temp space to keep last answer
  - So now the workflow will be
    - Input load to input
    - NBN ALU compute the newtwork
    - Output was charge
    - Then BNB ALU move to next point

# The Brain Recurent Network (BRN)

- recap:
  - The previos part we have NBN (Nerve Brain Network) that have n input and m output, which inside can keep its complexity with sparse network

- Concept:
  - We will built the NBN network to simulate Recurrent Neural Network.
  - Imagine now we have 2 different stucture of NBN, 1: prefontal cortex that heavy at sparse network to keep normal pattern, build aim for calculation. 2: hippocampus that heavy at sparse network for to keep memory recall pattern, aim to remember averthing with hierachy

- Workflow:
  - First Prefontal cortext will get input and previos stage from hippocampus, but this time this NBN will have 2 part of ouput, 1: for input prediection, second to send to Prefontal cortext (via CNS)
  - Then hippocampus will recive input from same input and CNS from prefontal cortext, then it compute the network to send back weight to prefontal cortext via (CNS)
  - Then prefontal cortext will get last memory and predict input again, making recursive network

- Back Propagation:
  - Both of brain will get same input (the key), and it will be gradient in every clock that predict it. Making strong path for that key
  - For Prefontal cortext and Hippocampus communication via CNS, This will be the main part, the input that hippocampus see act like a data leakage. We have to build this connecting more stong than that, to make complex intelligence. So we will make low quality decode for input and expand the CNS size to make it more stronger. Image -> map the group of thing that similar, The thought -> map the real thing we want.
  - The backpropagation will just be in clock order, move between prefontal cortext compute and hippocampuse call memory

- Back Propagation Workflow:
  - First, start at Prefontal cortext with ramdom previous input of hippocampus, predict the output.
  - Second, the prefontal cortex use backpagation on it loss from prediction and history-bit, update the NCN weight. While send Prefontal output part to hippocampus via CNS + input (low grade) to hippocampus, predict the output
  - Third, hippocampus backpopagation itself with history bit, while sending back it predict to prefontal cotext, loop everything again.
  - This will make efficiency, while the brain update it wieght, that time another brain waiting the current flow charging its output capacitro.

# The Central Nerve System (CNS)

- desctiption
  - because we have so less brain part, making we can heavy compute at this connector too while it don't reduce the efficiency.
  - using NCN group for translate between each part of brain
  - we can apply synape position computatation for more complexity of model (detail in the next topic)

# The Dynamic Synapse Position (DSP)

- Description
  - we will keep position for only 1D with binary number, keep in SRAM for each synapse
  - The targer (output) neural will call out its (1D poition + output to re call shanal number) to the group
  - Then the child neural will send it output (aW+b with opamp-like) \* distance with binary set opamp-like int that channel
  - then the traget neural will recieve it

- Detail
  - child neural can have 3-5 synapse
  - the backpropagation still use the same, we set input then set update_signal to true and send feedback in global chanel, it just require more logic to compute multiply distance

- Limitation
  - Because this method require chanel and specific module, making it's so expensive
  - But the position trade off is so good. The 1D position move will make each synapse more varaint, the data will convergence faster and stable.

# Short Term Memory Brain Network (STM) (Prototype)

- desctiption
  - Optional, this ideas still not stable
  - The current BRN still isn't enough, it just be a super short memory, it can pull only the group of catagory with so no external memory. input shift -> forget
  - So STM will be the pure capacitor network, act like LSTM layer, keeping short term context that stack from the Prefontal Cortex and Hippocampus.
  - Prefontal cortex: input (high quality) + hippocampus (CNS) + STM (CNS)
  - Hippocampus: input (low quality) + Prefontal cortext (CNS) + STM (CNS)
  - STM: Prefontal cotex (CNS) + Hippo campus (CNS)

# Routes Hierachy

- description
  - Because now we're in circuit, not a biology cell. We have so less ALU, and only 2D space. Making we have to arrange our neuro path.

- ideas:
  - Unit Routes, NCN will that is 2-3-3-2 scap will be keep.
  - L1 Routes, Using a 4bit for Unit Routes (16 capacity NCN)
  - L2 Routes, Using a 4bit for L1 Routes (256 capacity NCN)
  - Think like L1 Routes is horizontal and L2 Routes is vertical grid
  - To navigate NCN, we require 4bit, but to save, we optimize it

- Sequencail load data (binary wave)
  - First we set L1 and L2 path to activate only 1 Unit Routes NCN
  - Then we send output channel to it (the chanell that this NCN have to send output), Each NCN will have SRAM to save the wire path
  - Then go to next NCN
  - this load will be use only once to wiring them

- After sequencial load
  - Next after sequenc loading data, each Unit Routes will know which is their input capacitor position, their output charge capacitor, etc. (The Unit routes will keep only path, and the ALU will read this path automatically)

- Optimize
  - Each brain like Prefontal, Hippocampus, or STM will have special shcema to optimize it flow
  - Each brain will use this L1/L2 map for NCN, but may have special tool

# cell fire (optional)

- detail:
  - this concept is about fire pattern. From before that we talk about possitive feedback a bit, that the strong neural get strong feedback, get more strong weight. Pulling our all distribute from waek nerual. But there are more fire mechanism that said about "The group of neural that fire togaher regulary, it will be more wired together". So this topic is talking about how we capture those fire pattern and make it wire in the group level.

- theroy:
  - It didn't stable yet. My ideas is about STDP (spike timing dependent plasticity) of Hebbian rule Δw ∝ f(pre, post, timing), using it concept.
  - I will use

# WorkFlow Ideas now
